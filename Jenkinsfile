pipeline {
    agent {label 'windows'} {
		stage('Build') {
			echo 'Build windows'
		}
		stage('Tests') {
			echo 'Tests windows'
		}
		stage('Static analysis') {
			echo 'Static analysis windows'
		}
	}
}
