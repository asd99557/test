pipeline {
    agent {label 'windows'} {
	     stages {
		stage('Build') {
			echo 'Build windows'
		}
		stage('Tests') {
			echo 'Tests windows'
		}
		stage('Static analysis') {
			echo 'Static analysis windows'
		}
	}
}
